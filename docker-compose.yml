services:
  web:
    image: hesab:latest
    restart: unless-stopped
    ports:
      - "3001:3001"
    environment:
      - DATABASE_URL=file:/app/data/app.db
      - NODE_ENV=production
      - PORT=3001
    volumes:
      - ./data:/app/data
